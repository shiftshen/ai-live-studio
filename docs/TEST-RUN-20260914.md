# 2026-09-14 持续验收

本记录对应真实服务 http://127.0.0.1:8890。持续工具只读API、生产SQLite与进程信息，没有模拟注入、房间修改、打印请求或GitHub上传。

## 本次新增及验证

- `/Volumes/M2USB/Projects/ai-live-studio/scripts/acceptance-soak.ts`：可参数化总时长、采样间隔、平台、输出目录；一次后台观察同时给出30分钟双平台门槛和120分钟进程稳定门槛。
- `/Volumes/M2USB/Projects/ai-live-studio/tests/acceptance-soak.test.ts`：5/5通过，覆盖未完成禁止通过、connected但无真实事件、重启/断线失败保持、正常完成、采样遗漏。服务端TypeScript检查通过。
- 记录每次PID、uptime、RSS、数据库大小、各状态/动作队列计数及最老时间、房间状态/场次；按平台/房间/事件类型/礼物类型记录实际非历史live事件增量和首末时间。无数据间隔或断线明确记失败。
- 服务连续性与真实平台门槛分开：service通过只能证明采样端点和进程连续性；不会替代内存泄漏分析、队列处理正确性、真实礼物、连击或实体出纸验收。采样不能证明两个采样点之间没有短暂断线。

## 当前持续观察

起点2026-09-14 06:30:45.233 UTC，预计两小时终点08:30:45.233 UTC，PID75195。报告：`/Volumes/M2USB/Projects/ai-live-studio/var/evidence/acceptance-corrected/report.json`。实际完成状态只能读取报告，不依据预计时间。

PID57372运行期间TikTok不在线且跨越开发重启，已停止；PID71167发现PATCH默认值清空地址导致两平台断开，也已停止，失败证据保留。修复后重新连接两平台才启动当前观察。后续代码提交与现场运行版本须区分，不能把未部署补丁说成已长时验证。

## 既有负载证据的范围

`/Volumes/M2USB/Projects/ai-live-studio/scripts/load-test.ts`使用独立数据库，每秒批量注入100条模拟普通评论，共600秒/60000条。历史结果stored=60000、jobs=0、最大百条批次135ms。它没有并发HTTP查询、规则命中任务处理、供应商延时或真实采集性能验证。本次没有重复启动高资源负载，也没有据此宣称完整100/s业务链路通过。

## 重跑

从`/Volumes/M2USB/Projects/ai-live-studio`执行：

```sh
node --import tsx /Volumes/M2USB/Projects/ai-live-studio/scripts/acceptance-soak.ts --seconds=7200 --interval=60 --output=/Volumes/M2USB/Projects/ai-live-studio/var/evidence/acceptance-NEW-RUN
```

每次使用新的输出目录；持续观察必须保留完整首末时间与失败字段。开发重启会明确失败，不删除失败重新包装为成功。

自查：读取均为只读，不改房间、不触发实体动作；只统计origin=live且非historical记录；输出不含admin-token或overlayToken；不能把匿名身份事件当成可靠粉丝身份。

## 新增HTTP负载已完成

`/Volumes/M2USB/Projects/ai-live-studio/var/evidence/http-load-1789368121458/result.json`：600.006秒，60000请求全部接收并入库，36000任务，0错误；600次并发状态读取最大827ms，批次P95 370ms、最大840ms。仅独立模拟HTTP、认证、SQLite、20%规则匹配和状态查询，Worker关闭，无真实输出验证。
